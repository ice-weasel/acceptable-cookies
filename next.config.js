/** @type {import('next').NextConfig} */
const nextConfig = {}
// next.config.js
module.exports = {
    images: {
      domains: ['ichef.bbci.co.uk'], // Add any other allowed domains here
    },
  };
  